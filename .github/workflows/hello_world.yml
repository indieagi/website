name: "Tech Debt: Run Pre-Merge Automated Tests Here" 

on:
  pull_request:
    branches: [ main ]

jobs:
  hello_world_job:
    runs-on: ubuntu-latest

    steps:
    - name: Print hello world
      run: echo "Hello, world!"
